# Cordova 原生壳专用构建变量（`vite build --mode cordova` 自动加载）。
# 原生壳没有 Vite dev 代理/Electron 那样的本地反代，网络请求必须直接打到
# 已部署的公网后端。WebView 用 https://localhost 托管页面，请求必须走 HTTPS，
# 否则明文 http 会被浏览器当作 Mixed Content 直接拦掉。
# 线上 443 反代：/api、/admin -> Django；/translate、/click、/rei -> Flask 模型服务。
VITE_API_BASE=https://openshsid.zengyuxiang.cn/api
VITE_FLASK_BASE=https://openshsid.zengyuxiang.cn
VITE_DJANGO_ORIGIN=https://openshsid.zengyuxiang.cn
