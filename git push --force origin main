会覆盖远程历史，建议仅在必要时使用。

## 示例流程
1. 添加更改：`git add .`
2. 提交：`git commit -m "描述"`
3. 推送：`git push origin main`

## 常见问题
- **推送被拒绝（非快进）**：远程有您没有的提交。先 `git pull` 再推送。
- **需要先 `git pull --rebase`**：保持历史线性。
- **权限错误**：确保 SSH key 或 token 已配置。

## 查看远程仓库
